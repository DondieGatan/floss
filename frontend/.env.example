# Copy to .env.local for local dev pointed at a non-default backend, or set
# in your host's dashboard for deploys. Defaults to http://localhost:5101/api
# if unset, which is correct for local dev against the backend's default port.

VITE_API_BASE_URL=https://your-backend.onrender.com/api
